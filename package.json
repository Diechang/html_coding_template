{
  "name": "html_coding_template",
  "version": "0.0.1",
  "scripts": {
    "server": "http-server",
    "s": "npm run server",
    "sass": "sass",
    "sass-watch": "sass --watch scss:css --style=expanded --no-source-map",
    "dev": "run-p server sass-watch"
  },
  "dependencies": {
    "reset-css": "^5.0.2",
    "sass": "^1.77.8"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5"
  }
}
